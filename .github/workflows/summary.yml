name: Summary CI
on:
  repository_dispatch:
    types: [summary]
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:
jobs:
  release:
    name: Generate README
    runs-on: ubuntu-latest
    steps:
      - name: Generate README
        uses: upptime/uptime-monitor@v1
        with:
          command: "readme"
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
